/*let a = 5;
let b = 3;

let resultado = a + b;

console.log("A soma de " + a + " + " + b + " é igual a " + resultado);

  ________________________________________________________
  |  SOMA = +                                               |
  |  SUBTRAÇÃO = -                                          |
  |  MULTIPLICAÇÃO = *                                      |
  |  DIVISÃO = /                                            |
  |  MÓDULO = RESTO = %                                     |
  |  EXPONENCIAÇÃO = **                                     |
  |  ATRIBUIÇÃO COM ADIÇÃO = +=                             |
  |  INCREMENTO = ++                                        |
  |  COMPARAÇÃO = == (compara somente valores)              |
  |  ESTRITA IGUALDADE = === (compara valores e tipos)      |
  |  DESIGUALDADE = != (somente valores diferentes)         |
  |  ESTRITA DESIGUALDADE !== (valores e tipos diferentes)  |
  |  MAIOR QUE = >                                          |
  |  MENOR QUE = <                                          |
  |  MAIOR OU IGUAL = >=                                    |
  |  MENOR OU IGUAL = <=                                    |
  |  AND = &&                                               |
  |  OR = ||                                                |
  |  NOT = !                                                |
  |_________________________________________________________|
 

let a = 7;
let b = 3;
let resto = a%b;
console.log("A divisão de " + a + " por " + b + " tem resto igual a " + resto);

let c = 2;
let d = 3;
let resultado = c**d;
console.log(c + " elevado a " + d + " é igual a " + resultado);

let a = 5;
a++;
console.log(a);

let a = 5;
let b = '5';
console.log(a!==b);

*/

let a = 5;
let b = 3;
console.log((a > 4) && (b < 4));